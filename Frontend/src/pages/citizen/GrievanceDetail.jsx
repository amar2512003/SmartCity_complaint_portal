import {Link,useParams} from 'react-router-dom'; export default function GrievanceDetail(){const {id}=useParams();return <div className="form-panel panel"><span className="eyebrow" style={{color:'var(--primary)'}}>Report #{id}</span><h2>Grievance details</h2><p className="subtitle">Detailed view is available from the dashboard. This page is ready for future expansion.</p><Link className="button" to="/dashboard">← Back to dashboard</Link></div>}
