PORT=5001
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smart_city
JWT_SECRET=change_this_secret

# Optional: only used when running `npm run migrate` against Supabase.
# Leave blank for local dev (DATABASE_URL above is used instead).
MIGRATION_DATABASE_URL=

# The deployed frontend's origin, e.g. https://smartcity.vercel.app
# Leave blank locally to allow any origin.
FRONTEND_URL=

# Email OTP settings - fill these in before using signup verification.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=nonplusseddevil25@gmail.com
SMTP_PASS=vhdbplvaszoldzro

# Groq AI assistant (Citizen Desk chatbot)
# Get a free key at https://console.groq.com/keys and paste it below.
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b
