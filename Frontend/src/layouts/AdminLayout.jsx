import {Link,Outlet,useLocation} from 'react-router-dom'; import {useTranslation} from 'react-i18next'; import {useAuth} from '../context/AuthContext'; import Footer from '../components/Footer'; import LanguageToggle from '../components/LanguageToggle'; import brandMark from '../assets/brand-mark.png';
export default function AdminLayout(){const {t}=useTranslation('admin');const {logout}=useAuth();const location=useLocation();return <div className="shell" style={{display:'flex',flexDirection:'column'}}><header className="app-nav"><div className="nav-inner"><Link className="brand" to="/admin"><img src={brandMark} alt="SmartCity" className="brand-mark" /><span>Samadhan. <small style={{fontFamily:'DM Sans',fontSize:11,color:'#7b8790'}}>{t('nav.badge')}</small></span></Link><nav className="nav-links"><Link className={location.pathname==='/admin'?'active':''} to="/admin">{t('nav.grievanceDesk')}</Link></nav><div className="nav-user"><LanguageToggle /><div className="avatar">A</div><span className="user-name">{t('nav.administrator')}</span><button className="ghost-btn mobile-hide" onClick={logout}>{t('nav.signOut')}</button></div></div></header><main className="container page" style={{flex:1}}><Outlet/></main><Footer/></div>}
