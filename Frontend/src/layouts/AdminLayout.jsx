import {Link,Outlet,useLocation} from 'react-router-dom'; import {useAuth} from '../context/AuthContext'; import Footer from '../components/Footer'; import brandMark from '../assets/brand-mark.png';
export default function AdminLayout(){const {logout}=useAuth();const location=useLocation();return <div className="shell" style={{display:'flex',flexDirection:'column'}}><header className="app-nav"><div className="nav-inner"><Link className="brand" to="/admin"><img src={brandMark} alt="SmartCity" className="brand-mark" /><span>SmartCity <small style={{fontFamily:'DM Sans',fontSize:11,color:'#7b8790'}}>ADMIN</small></span></Link><nav className="nav-links"><Link className={location.pathname==='/admin'?'active':''} to="/admin">Grievance Desk</Link></nav><div className="nav-user"><div className="avatar">A</div><span className="user-name">Administrator</span><button className="ghost-btn mobile-hide" onClick={logout}>Sign out</button></div></div></header><main className="container page" style={{flex:1}}><Outlet/></main><Footer/></div>}